/****************************** 总结 *********************************/ 
for (/* 修改点0: 确定是顺序还是逆序遍历 */) {
    while (st.empty() == false) {
        if (/* 修改点1: 确定是递增栈还是递减栈 */) {
            /* 修改点2 :压栈前操作 */
            break;
        } else {
            /* 修改点3: 弹栈前操作 */
            st.pop();
        }
    }

    /* 修改点4: 当前单调栈维护 */

    st.push(i);
}

if (顺序遍历 + 递减栈 or 反序遍历 + 递增栈) {
    /* 修改点5: 最终单调栈维护 */   
}

/********************************************************************/